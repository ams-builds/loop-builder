# loop-builder
A Claude skill that walks you step-by-step through turning a workflow you already repeat with Claude into a real Claude loop, a repeatable process with a trigger, a checklist, a state file, and a review point, instead of a repeated one-off prompt.
